<%= turbo_stream_from current_restaurant, :kitchen %>
<div data-controller="kitchen-chime"><%= render "kitchen/chime" %></div>

<div class="flex items-center justify-between mb-6">
  <h1 class="nb-heading text-2xl">Cocina</h1>
  <%= link_to "Historial", history_kitchen_path, class: "btn-secondary text-xs" %>
</div>

<%= render "kitchen/board", order_items: @order_items %>
