<button type="button" class="btn-secondary text-xs"
        data-controller="history-back" data-action="click->history-back#back"
        data-history-back-fallback-url-value="<%= local_assigns[:fallback] %>">
  &larr; Volver
</button>
